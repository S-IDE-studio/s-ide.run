---
import Layout from '../layouts/Layout.astro';
import { translations } from '../i18n/ui';

const locale = Astro.currentLocale || 'ja';
const t = translations[locale as keyof typeof translations];

const terminalCode = `$ Terminal 1 | Terminal 2 | Terminal 3
────────────────────────────────
~/project $ npm run dev

~/project $ git status

~/project $ npm test`;

const monacoCode = `function greet(name: string) {
  return \`Hello, \${name}!\`;
}

// IntelliSenseが自動補完
greet("World");`;

const gitCode = `📁 main
  ✏️ src/app.tsx (Modified)
  ✏️ src/utils.ts (Modified)

Changes (2):
  + 12 lines
  - 3 lines

[Commit] [Push] [Pull]`;

const deckCode = `~/workspace/project-a
src/
components/
utils/`;
---

<Layout title="機能 - S-IDE">
  <section class="py-20 sm:py-32">
    <div class="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8">
      <!-- Header -->
      <div class="text-center mb-16">
        <h1 class="text-4xl sm:text-5xl font-bold mb-4">
          機能
        </h1>
        <p class="text-xl text-[#94a3b8]">
          AI開発を加速するパワフルな機能
        </p>
      </div>

      <!-- Core Features -->
      <div class="space-y-16">
        <!-- Multi-Terminal -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 items-center">
          <div>
            <h2 class="text-2xl font-bold mb-4">マルチターミナル</h2>
            <p class="text-[#94a3b8] mb-4">
              複数のターミナルを同時に使用可能。ドラッグで並び替え、各デッキごとに独立したセッション管理。
            </p>
            <ul class="space-y-2 text-sm text-[#94a3b8]">
              <li class="flex items-start gap-2">
                <svg class="h-5 w-5 text-blue-400 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
                各デッキに複数のターミナルを追加
              </li>
              <li class="flex items-start gap-2">
                <svg class="h-5 w-5 text-blue-400 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
                ドラッグ＆ドロップで並び替え
              </li>
              <li class="flex items-start gap-2">
                <svg class="h-5 w-5 text-blue-400 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
                xterm.js搭載で本格的な端末エミュレーション
              </li>
            </ul>
          </div>
          <div class="bg-[#1e293b] rounded-xl border border-white/10 p-6">
            <pre class="text-sm text-[#94a3b8] overflow-x-auto"><code>{terminalCode}</code></pre>
          </div>
        </div>

        <!-- Monaco Editor -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 items-center">
          <div class="order-2 lg:order-1 bg-[#1e293b] rounded-xl border border-white/10 p-6">
            <pre class="text-sm text-blue-400 overflow-x-auto"><code>{monacoCode}</code></pre>
          </div>
          <div class="order-1 lg:order-2">
            <h2 class="text-2xl font-bold mb-4">Monaco Editor</h2>
            <p class="text-[#94a3b8] mb-4">
              VS Codeと同じエディタエンジン。シンタックスハイライト、IntelliSense、リッチな編集機能を提供。
            </p>
            <ul class="space-y-2 text-sm text-[#94a3b8]">
              <li class="flex items-start gap-2">
                <svg class="h-5 w-5 text-blue-400 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
                100以上の言語をサポート
              </li>
              <li class="flex items-start gap-2">
                <svg class="h-5 w-5 text-blue-400 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
                高度なコード補完
              </li>
              <li class="flex items-start gap-2">
                <svg class="h-5 w-5 text-blue-400 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
                複数カーソル、マルチ選択
              </li>
            </ul>
          </div>
        </div>

        <!-- Git Integration -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 items-center">
          <div>
            <h2 class="text-2xl font-bold mb-4">Git統合</h2>
            <p class="text-[#94a3b8] mb-4">
              ステータス確認、Diff表示、コミット、プッシュ/プル、ブランチ管理など完全なGitワークフローをサポート。
            </p>
            <ul class="space-y-2 text-sm text-[#94a3b8]">
              <li class="flex items-start gap-2">
                <svg class="h-5 w-5 text-blue-400 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
                複数リポジトリ対応
              </li>
              <li class="flex items-start gap-2">
                <svg class="h-5 w-5 text-blue-400 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
                サイドバイサイドDiff表示
              </li>
              <li class="flex items-start gap-2">
                <svg class="h-5 w-5 text-blue-400 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
                ブランチの作成・切り替え
              </li>
            </ul>
          </div>
          <div class="bg-[#1e293b] rounded-xl border border-white/10 p-6">
            <pre class="text-sm text-[#94a3b8] overflow-x-auto"><code>{gitCode}</code></pre>
          </div>
        </div>

        <!-- Context Manager -->
        <div class="bg-gradient-to-br from-blue-500/10 to-purple-500/10 rounded-2xl border border-blue-500/20 p-8">
          <h2 class="text-2xl font-bold mb-4 text-center">Context Manager</h2>
          <p class="text-[#94a3b8] mb-6 text-center">
            AIセッションの「コンテキストロット」を防止するための専用機能
          </p>
          <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
            <div class="bg-[#1e293b] rounded-lg p-4 text-center">
              <div class="text-2xl font-bold text-blue-400 mb-1">85</div>
              <div class="text-xs text-[#94a3b8]">Health Score</div>
            </div>
            <div class="bg-[#1e293b] rounded-lg p-4 text-center">
              <div class="text-2xl font-bold text-green-400 mb-1">23%</div>
              <div class="text-xs text-[#94a3b8]">Topic Drift</div>
            </div>
            <div class="bg-[#1e293b] rounded-lg p-4 text-center">
              <div class="text-2xl font-bold text-purple-400 mb-1">142</div>
              <div class="text-xs text-[#94a3b8]">Messages</div>
            </div>
            <div class="bg-[#1e293b] rounded-lg p-4 text-center">
              <div class="text-2xl font-bold text-orange-400 mb-1">3</div>
              <div class="text-xs text-[#94a3b8]">Snapshots</div>
            </div>
          </div>
        </div>

        <!-- Deck System -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 items-center">
          <div class="order-2 lg:order-1 bg-[#1e293b] rounded-xl border border-white/10 p-6">
            <div class="flex gap-2 mb-4">
              <div class="px-4 py-2 bg-blue-500/20 rounded-lg text-blue-400 text-sm">Project A</div>
              <div class="px-4 py-2 bg-[#0f172a] rounded-lg text-[#94a3b8] text-sm">Project B</div>
              <div class="px-4 py-2 bg-[#0f172a] rounded-lg text-[#94a3b8] text-sm">+</div>
            </div>
            <pre class="text-sm text-[#94a3b8]"><code>{deckCode}</code></pre>
          </div>
          <div class="order-1 lg:order-2">
            <h2 class="text-2xl font-bold mb-4">デッキシステム</h2>
            <p class="text-[#94a3b8] mb-4">
              固定ルートパスを持つ名前付きワークスペース。複数プロジェクトの効率的な管理を実現。
            </p>
            <ul class="space-y-2 text-sm text-[#94a3b8]">
              <li class="flex items-start gap-2">
                <svg class="h-5 w-5 text-blue-400 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
                プロジェクトごとの設定を保持
              </li>
              <li class="flex items-start gap-2">
                <svg class="h-5 w-5 text-blue-400 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
                最大3つのデッキを同時表示
              </li>
            </ul>
          </div>
        </div>
      </div>

      <!-- CTA -->
      <div class="mt-20 text-center">
        <a
          href="/download"
          class="inline-flex items-center gap-2 px-8 py-4 bg-gradient-to-r from-[#3b82f6] to-[#8b5cf6] rounded-lg text-white font-semibold hover:shadow-lg hover:shadow-blue-500/25 transition-all"
        >
          今すぐ試す
          <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4" />
          </svg>
        </a>
      </div>
    </div>
  </section>
</Layout>
