---
---

<section class="relative bg-black">
  {/* Grid Container */}
  <div class="relative z-10 mx-auto flex flex-col screenshots-container">
    {/* Label */}
    <p class="text-[#666666] font-semibold tracking-wider label">
      SCREENSHOTS
    </p>

    {/* Pattern 1: Text Left + Image Right */}
    <article class="flex items-center screenshot-row">
      {/* Text */}
      <div class="screenshot-text">
        <h2 class="text-white font-bold mb-4">エージェント管理</h2>
        <p class="text-[#bbbbbb] leading-[1.5]">
          複数のAIエージェントを統合して一元管理
        </p>
      </div>
      {/* Image */}
      <div class="flex items-center justify-center screenshot-placeholder" role="img" aria-label="エージェント管理画面のスクリーンショット">
        <span class="text-[#666666] text-xs font-semibold">Screenshot</span>
      </div>
    </article>

    {/* Pattern 2: Image Left + Text Right */}
    <article class="flex items-center screenshot-row">
      {/* Image */}
      <div class="flex items-center justify-center screenshot-placeholder" role="img" aria-label="ローカル実行画面のスクリーンショット">
        <span class="text-[#666666] text-xs font-semibold">Screenshot</span>
      </div>
      {/* Text */}
      <div class="screenshot-text">
        <h2 class="text-white font-bold mb-4">ローカル実行</h2>
        <p class="text-[#bbbbbb] leading-[1.5]">
          データをローカル環境で安全に処理
        </p>
      </div>
    </article>

    {/* Pattern 3: Full Width Image + Text Below */}
    <article class="flex flex-col screenshot-full">
      {/* Image */}
      <div class="flex items-center justify-center screenshot-placeholder-full" role="img" aria-label="マルチデバイス対応画面のスクリーンショット">
        <span class="text-[#666666] text-xs font-semibold">Screenshot</span>
      </div>
      {/* Text */}
      <div class="text-center screenshot-text-full">
        <h2 class="text-white font-bold mb-3">マルチデバイス対応</h2>
        <p class="text-[#bbbbbb] leading-[1.5]">
          あらゆるデバイスからブラウザでアクセス可能
        </p>
      </div>
    </article>
  </div>

  {/* Responsive styles */}
  <style scoped>
    /* Screenshots container responsive max-width */
    .screenshots-container {
      max-width: 916px;
      padding: 3.75rem 0;
      gap: 18px;
    }

    /* Label responsive styles */
    .label {
      font-size: 14px;
      letter-spacing: 2px;
    }

    /* Screenshot row (2-column layout) */
    .screenshot-row {
      gap: 9px;
      min-height: 320px;
    }

    .screenshot-text {
      width: 453px;
      /* 3 columns: 145px + 9px + 145px + 9px + 145px = 453px */
    }

    .screenshot-placeholder {
      width: 454px;
      /* Remaining: 916px - 9px - 453px = 454px */
      height: 320px;
      background-color: #1a1a1a;
      border: 1px solid #333333;
    }

    .screenshot-text h2 {
      font-size: 24px;
    }

    .screenshot-text p {
      font-size: 14px;
    }

    /* Full width screenshot */
    .screenshot-full {
      gap: 9px;
    }

    .screenshot-placeholder-full {
      width: 916px;
      height: 400px;
      background-color: #1a1a1a;
      border: 1px solid #333333;
    }

    .screenshot-text-full {
      width: 916px;
    }

    .screenshot-text-full h2 {
      font-size: 24px;
    }

    .screenshot-text-full p {
      font-size: 14px;
    }

    /* Mobile (< 480px) - 1 column, 2-column rows become 1-column */
    @media (max-width: 479px) {
      .screenshots-container {
        max-width: 100%;
        padding-left: 18px;
        padding-right: 18px;
        padding-top: 2rem;
        padding-bottom: 2rem;
        gap: 18px;
      }

      .label {
        font-size: 12px;
        letter-spacing: 1px;
      }

      /* Change 2-column layout to 1-column */
      .screenshot-row {
        flex-direction: column;
        min-height: auto;
        gap: 9px;
      }

      .screenshot-text {
        width: 100%;
      }

      .screenshot-text h2 {
        font-size: 20px;
      }

      .screenshot-text p {
        font-size: 13px;
      }

      .screenshot-placeholder {
        width: 100%;
        height: 200px;
      }

      /* Full width screenshot adjustments */
      .screenshot-full {
        gap: 9px;
      }

      .screenshot-placeholder-full {
        width: 100%;
        height: 200px;
      }

      .screenshot-text-full {
        width: 100%;
      }

      .screenshot-text-full h2 {
        font-size: 20px;
      }

      .screenshot-text-full p {
        font-size: 13px;
      }
    }

    /* Small (480-616px) - 2 columns */
    @media (min-width: 480px) and (max-width: 616px) {
      .screenshots-container {
        max-width: 299px;
        padding: 2.5rem 0;
        gap: 18px;
      }

      .label {
        font-size: 13px;
        letter-spacing: 1.5px;
      }

      /* Change to 1-column vertical layout */
      .screenshot-row {
        flex-direction: column;
        min-height: auto;
        gap: 9px;
      }

      .screenshot-text {
        width: 100%;
      }

      .screenshot-text h2 {
        font-size: 18px;
      }

      .screenshot-text p {
        font-size: 12px;
      }

      .screenshot-placeholder {
        width: 100%;
        height: 200px;
      }

      /* Full width - still full width of 2-column container */
      .screenshot-full {
        gap: 9px;
      }

      .screenshot-placeholder-full {
        width: 299px;
        height: 250px;
      }

      .screenshot-text-full {
        width: 299px;
      }

      .screenshot-text-full h2 {
        font-size: 18px;
      }

      .screenshot-text-full p {
        font-size: 12px;
      }
    }

    /* Medium (617-915px) - 4 columns */
    @media (min-width: 617px) and (max-width: 915px) {
      .screenshots-container {
        max-width: var(--grid-4-col);
        padding: 3rem 0;
        gap: 18px;
      }

      .label {
        font-size: 13px;
        letter-spacing: 1.5px;
      }

      .screenshot-row {
        min-height: 300px;
        gap: 9px;
        justify-content: center;
        /* Center 607px content in 617px container */
      }

      .screenshot-text {
        width: 299px;
        /* 2 columns: 145px + 9px + 145px = 299px */
      }

      .screenshot-text h2 {
        font-size: 22px;
      }

      .screenshot-text p {
        font-size: 13px;
      }

      .screenshot-placeholder {
        width: 299px;
        /* 2 columns: 145px + 9px + 145px = 299px */
        height: 300px;
      }

      /* Full width adjustments */
      .screenshot-full {
        gap: 9px;
        justify-content: center;
      }

      .screenshot-placeholder-full {
        width: 607px;
        /* 4 columns: 145px + 9px + 145px + 9px + 145px + 9px + 145px = 607px */
        height: 320px;
      }

      .screenshot-text-full {
        width: 607px;
      }

      .screenshot-text-full h2 {
        font-size: 22px;
      }

      .screenshot-text-full p {
        font-size: 13px;
      }
    }

    /* Large (916px+) - 6 columns */
    @media (min-width: 916px) {
      .screenshots-container {
        max-width: 916px;
        padding: 3.75rem 0;
        gap: 18px;
      }

      .label {
        font-size: 14px;
        letter-spacing: 2px;
      }

      .screenshot-row {
        min-height: 320px;
        gap: 9px;
      }

      .screenshot-text {
        width: 453px;
        /* 3 columns: 145px * 3 + 9px * 2 = 453px */
      }

      .screenshot-text h2 {
        font-size: 24px;
      }

      .screenshot-text p {
        font-size: 14px;
      }

      .screenshot-placeholder {
        width: 454px;
        /* Remaining: 916px - 9px - 453px = 454px */
        height: 320px;
      }

      /* Full width at 6 columns */
      .screenshot-full {
        gap: 9px;
      }

      .screenshot-placeholder-full {
        width: 916px;
        height: 400px;
      }

      .screenshot-text-full {
        width: 916px;
      }

      .screenshot-text-full h2 {
        font-size: 24px;
      }

      .screenshot-text-full p {
        font-size: 14px;
      }
    }
  </style>
</section>
